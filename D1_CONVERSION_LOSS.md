# D1 数据转换损失诊断

`training_started=false`; `inference_rerun=false`

本诊断只隔离数据转换因子，不评价方法有效性，不把剩余差距直接归给某个模型、训练或协议因素。全程为 CPU 读取、重建和指标计算；没有训练、推理或 GPU 任务。

## 0. 环境、输入哈希、原始数据定位结果

- 指标执行代码提交：`3e6cb4d91b7ee425cdf3b16e54df0ebd03ff364a`（报告/来源元数据随后单独提交）。
- 远端仓库：`/hy-tmp/NuSeg`；结果目录：`/hy-tmp/NuSeg/workdir/audits/d1_conversion_loss_20260808`。
- 转换 test GT：`/hy-tmp/NuSeg/data/PanNuke/test`；2607 PNG + 2607 JSON；JSON 清单 SHA256 `b2d33ab547fecdf50024c0953b8b451773fa788aa56cfb70d1153d160b0f04e1`。
- Visual 预测 NPY 清单 SHA256：`00b99be6fc116ab1199e093008d8235dd189e0503fd6aa8c2a93426780bbb70b`。
- Exp5 预测 NPY 清单 SHA256：`54da0aea223896d7d22f9ab536c51c8b46626da686bd9476b5ec31f41b1dd7f9`。

原始数据定位按预注册顺序执行：

1. 远端全文件系统搜索 `images.npy`、`masks.npy`、`types.npy`、`fold*`、`Fold*`、`pannuke*` 及常见压缩包：`NOT_FOUND`。
2. 检查 `/root/Downloads`、`~/.cache`、`/home`、`/tmp`、`/var/tmp`、`/hy-tmp`：`NOT_FOUND`；仅发现既有审计报告，不含原始数组。
3. 本机 `/Users/yizheng001` 搜索原始数组和压缩包：`NOT_FOUND`。
4. Warwick/TIA 官方旧下载页已迁移且当前没有直接 Fold 下载链接。固定版本公开镜像 `RationAI/PanNuke` 的生成脚本明确从原始 `images.npy`、`masks.npy`、`types.npy` 读取，按五个类别通道和原始实例 ID 展开为逐实例二值 mask。服务器直连 `huggingface.co` 超时，因此通过可达的 `hf-mirror.com` 解析同一固定 revision 下载。

下载记录：

- 规范来源 URL：`https://huggingface.co/datasets/RationAI/PanNuke/resolve/b9a02ac/data/fold3-00000-of-00001.parquet`
- 实际检索 URL：`https://hf-mirror.com/datasets/RationAI/PanNuke/resolve/b9a02ac/data/fold3-00000-of-00001.parquet`
- revision：`b9a02ac839d0383bdd3d023b56270ff402b1417f`
- linked ETag / 文件 SHA256：`5684f09517e81ff18e570608a54741e4e6715a93cbe08e32dbec3d60513457a0`
- 文件大小：289,256,878 bytes
- 实际 schema：2722 行；RGB PNG；逐实例 1-bit PNG mask；并行五类标签；19 类 tissue ID。
- Fold3 实例总数：66,654（由逐实例列表直接计数）。

任务说明引用的第三方预处理 README 称 Fold3 为 2723 张，但固定公开数据、其 metadata 和使用该数据的文献均给出 2722 张；本诊断报告实际读取值 2722，不以 2723 补齐或推测缺失样本。PanNuke 公布的全数据核总数为 189,744；未找到权威的 Fold3 单折核数，故单折官方对照写为 `NOT_FOUND`，不把 66,654 冒充官方公布值。

## 1. 索引映射

映射使用解码后的 `uint8[256,256,3]` RGB 像素字节 SHA256，不使用 mask。

| 检查 | 结果 |
|---|---:|
| 原始 Fold3 图像 | 2722 |
| 转换 test 图像 | 2607 |
| 精确哈希匹配 | 2607 / 2607（100%） |
| 原始侧重复图像哈希 | 0 |
| 转换侧重复图像哈希 | 0 |
| 未匹配转换图像 | 0 |
| 每张匹配图 `max_abs_diff` | 0 |
| 未进入转换 test 的原始图像 | 115 |

因此匹配率高于 95% 停止门槛，且预测输入图像没有被转换改写。`mapping_v2/index_mapping.json` 是 2607 项双射，SHA256 `39847f31717690c9aaa9f1a84ff82a195b4ba3bc57cfe17653c8a72a21ecd4bb`。

## 2. 样本级损失

原始 2722 张，转换后 2607 张，共剔除 115 张：114 张 `GT_EMPTY`，1 张有且仅有 1 个原始实例，但所有外轮廓均未通过 `contourArea >= 10`/合法多边形条件。剔除样本原始实例数均值 0.00870、中位数 0、最大值 1；保留样本均值 25.5669、中位数 21、最大值 214。样本剔除明确集中在空/极低密度图像。

组织分布并不均匀。最高剔除率为 Cervix 16.28%（14/86）、Head & Neck 11.72%（17/145）、Bladder 10.94%（7/64）、Colon 10.32%（51/494）；Esophagus、Liver、Lung、Ovarian、Pancreatic、Skin、Stomach、Uterus均为 0%。完整 19 组织计数见 `conversion_v2/sample_loss_by_organ.csv`。

样本选择因而系统性依赖组织中的空 GT 比例，但它几乎没有删除已标注核：115 张中只有 1 个原始实例。

## 3. 实例级损失

严格按 D1.3 预注册口径：先合并五类为二值前景，再做 8 连通域计数。

| 统计量 | 值 |
|---|---:|
| 匹配图原始二值连通域总数 `N_orig` | 56,067 |
| 转换 JSON 实例总数 `N_conv` | 54,497 |
| 绝对损失 | 1,570 |
| 相对损失 | 2.8002% |
| 逐图损失 min / median / mean / max / std | 0 / 0 / 0.6022 / 8 / 0.9044 |
| 损失 > 0 的图像比例 | 41.0817% |
| 损失 > 10 的图像数 | 0 |

补充的实例身份口径不能与上表混用：匹配图有 66,653 个原始逐通道实例，转换后只有 54,497 个多边形，身份数差 12,156（18.2377%）。差异主要来自多个原始实例先并入同一个二值连通域，而不是只有面积过滤。另有 66,653/66,653 个原始实例类别标签在转换 JSON 中不可恢复；所有转换实例均写成常量 `category_id=1`，类别信息销毁率 100%。

## 4. 尺寸分布损失

“保留”主口径为原始实例与重建实例间最大化 IoU 的一对一 Hungarian 匹配，并要求交集大于 0；同时报告严格 `IoU > 0.5`。该定义避免一个合并多边形被重复算作保留多个原始实例。

| 原始像素面积 | 原始实例 | 一对一保留 | 保留率 | `IoU > 0.5` 保留 | 严格保留率 |
|---|---:|---:|---:|---:|---:|
| [1,5) | 355 | 0 | 0.00% | 0 | 0.00% |
| [5,10) | 534 | 0 | 0.00% | 0 | 0.00% |
| [10,20) | 694 | 30 | 4.32% | 30 | 4.32% |
| [20,50) | 1,870 | 1,457 | 77.91% | 1,455 | 77.81% |
| [50,100) | 3,882 | 3,119 | 80.35% | 3,094 | 79.70% |
| [100,200) | 9,322 | 7,547 | 80.96% | 7,450 | 79.92% |
| [200,+∞) | 49,996 | 42,342 | 84.69% | 40,880 | 81.77% |

面积 <20 的原始实例为 1,583/66,653 = 2.3750%，低于预注册 15% 矛盾门槛，未触发该矛盾。`[10,20)` 的低保留率来自原始像素面积与转换端 `cv2.contourArea` 的口径差异，加上先二值合并再轮廓化；不能把“原始像素面积 ≥10”等同于“轮廓面积 ≥10”。曲线见 `conversion_v2/size_retention_curve.png` 和 CSV。

## 5. 像素级与拓扑损失

转换 GT 使用 `evaluation.recompute_from_npy.load_gt_like_test_py`，它是 `test.py:880-926` 解码逻辑的逐语句 CPU 端口。

| 像素/实例指标 | 值 |
|---|---:|
| 原始前景像素 | 30,220,645 |
| 重建前景像素 | 30,219,185 |
| 全局前景 IoU | 0.999008 |
| 重建/原始前景像素比 | 0.999952 |
| 逐图前景 IoU，均值 / 中位数 / 最小值 | 0.998966 / 0.999611 / 0.927966 |
| 原始实例 vs 重建实例 AJI（Kumar），均值 | 0.804918 |

像素形状几乎不变，但实例拓扑显著变化：7,110 个转换实例与多个原始实例相交，涉及 17,723 个原始实例；原始 22 个空洞中，17 个所在实例在重建中出现填洞；43 个原始多部件实例中 4 个被拆到多个转换实例。原始实例边界点均值 67.66，转换多边形顶点均值 36.34；全局顶点数/原始边界点数为 0.4391（该全局比同时受实例合并影响，不解释为单实例简化率）。

内部一致性通过：JSON `contourArea` 最小值 10.0，重建像素面积最小值 17，多边形最少 3 顶点。相邻重建实例最近边界距离在 54,394 个可解析实例上最小 2.0、中位数 6.403、均值 11.762 像素；k=128 邻域内仍有 60 个实例未解析到其他边界，记为 `UNVERIFIED`，不纳入距离分布。

官方 PanNuke `binarize()` 对跨类别重叠像素采用通道 0→4、通道内升序 ID、后写覆盖的规则；原始镜像生成顺序相同。匹配图共有 1,530 个这种重叠像素，所有原始 GT 重评估均按官方规则处理。

## 6. 用原始 GT 重评估的四组指标

同一组不可变预测分别对转换 GT、全部原始 GT、原始面积 ≥10 GT 评估。PQ 使用严格 `IoU > 0.5`；tissue-macro bPQ 沿用 P0.3/PanNuke 口径；AJI 为 Kumar greedy。

| 方法 / 指标 | 转换 GT | 原始 GT | Δ（原始−转换） | 原始面积≥10 GT | Δ≥10（≥10−转换） |
|---|---:|---:|---:|---:|---:|
| Visual bPQ | 0.621168 | 0.593472 | -0.027696 | 0.597959 | -0.023209 |
| Visual AJI | 0.617242 | 0.583970 | -0.033272 | 0.584752 | -0.032491 |
| Exp5 bPQ | 0.627487 | 0.599821 | -0.027666 | 0.604354 | -0.023133 |
| Exp5 AJI | 0.627020 | 0.592449 | -0.034572 | 0.593098 | -0.033922 |

转换 GT 的四个数与 P0.3 已报告值逐项差异为 0。官方 PQ 端口与独立实现的全任务最大逐图绝对差小于 `1e-6`，两道复核门均通过。

Visual 的总 bPQ 差分可按额外口径拆为：纳入面积 <10 原始实例的增量 `0.593472 - 0.597959 = -0.004487`；面积 ≥10 原始 GT 与转换 GT 的剩余差 `0.597959 - 0.621168 = -0.023209`。后者仍混合了二值跨类合并、实例身份丢失和轮廓化，不能继续归给单一损失点。Exp5 对应为 `-0.004533` 与 `-0.023133`。

公平性限制：模型训练时从未见过被过滤的小核，因此原始 GT 分数不代表模型真实上限，只用于数据转换因子归因。

## 7. 预注册判读结论

Visual `Δ = -0.027696 < -0.02`；Exp5 `Δ = -0.027666 < -0.02`。严格按预注册阈值，两者均判定：

`GT_CONVERSION_INFLATES`

即转换 GT 系统性更容易；它没有造成当前低分，反而抬高了报告分数。阈值在执行中未更改。

## 8. 归因结论

Visual 的文献低位差距为 `0.6596 - 0.621168 = 0.038432`。数据转换作为“惩罚”可回收的 bPQ 为 0；它反而掩盖 `0.027696`。若只把相同已存盘预测换成原始 GT，差距扩大为 `0.6596 - 0.593472 = 0.066128`。

因此，0.0384 已观察差距中不能归因为“转换 GT 惩罚模型”的部分是全部 0.0384；但 D1 不能把这 0.0384 或扩大的 0.0661 直接命名为模型架构损失。剩余差距归属为 `UNKNOWN`，可能还包含训练不足、阈值协议错配、架构、单折与文献三折平均等未隔离因素。

## 9. 可复算产物

- `mapping_v2/index_mapping.json`：2607 项精确双射；同目录含 `source_provenance.json`。
- `conversion_v2/per_image_conversion_loss.csv`：2607 行。
- `conversion_v2/size_retention_curve.csv` 和 PNG。
- `conversion_v2/removed_samples.csv`、`sample_loss_by_organ.csv`、`conversion_summary.json`、`topology_summary.json`。
- `reeval/reeval_original_gt_visual_baseline.csv`：2607 行。
- `reeval/reeval_original_gt_exp5.csv`：2607 行。
- `reeval/reeval_original_gt_area_ge10_*.csv` 及转换 GT 交叉复核 CSV。
- 根目录 `SHA256SUMS.txt`：覆盖报告及上述全部最终产物；第一版 `conversion/` 作为运行证据保留，但最终结论使用 `conversion_v2/`。

代码入口：`diagnostics/rebuild_index_mapping.py`、`diagnostics/quantify_conversion_loss.py`、`diagnostics/reeval_against_original.py`。三个入口均打印 `[DIAG_CONFIG]`，拒绝覆盖非空输出目录，并且没有模型、训练或推理路径。
