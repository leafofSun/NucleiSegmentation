# ============================================================
# Core dependencies — NucleiSegmentation (my_env)
# Generated by scanning all .py import statements in project.
# PyTorch family (torch / torchvision / torchaudio) is NOT
# included here; install separately per CUDA version.
# ============================================================

# ---------- Core scientific / image ----------
numpy
Pillow
opencv-python-headless
albumentations

# ---------- Visualization / metrics ----------
matplotlib
scipy
scikit-image
scikit-learn
pycocotools
tqdm

# ---------- Text / encoding utilities ----------
ftfy
regex
packaging

# ---------- Runtime utilities ----------
python-dotenv
tensorboard

# ============================================================
# Special packages (not on regular PyPI)
# ============================================================
# After installing the packages above, run:
#
#   # 1. CLIP (local subdirectory — part of this repo)
#   pip install -e ./CLIP/CLIP-main
#
#   # 2. CONCH (external git repo — histopathology foundation model)
#   pip install git+https://github.com/Mahmoodlab/CONCH.git
#
# ============================================================
